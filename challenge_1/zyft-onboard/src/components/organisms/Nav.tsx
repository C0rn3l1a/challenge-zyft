import logo from '../../assets/logo.svg';

export function Nav(_props: any) {
    return <nav className="flex items-center justify-center w-full p-4 bg-white">
        <img src={logo} />
    </nav>;
}
